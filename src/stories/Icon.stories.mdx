import { Meta, ArgsTable, Story, Canvas } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { linkTo } from "@storybook/addon-links";
import Icon, { types, iconNames, sizes } from "../components/common/Icon.vue";

<Meta
  title="Components / Icon"
  component={Icon}
  argTypes={{
    type: {
      options: types,
      control: { type: "inline-radio" },
    },
    name: {
      options: iconNames,
      control: { type: "select" },
    },
    size: {
      options: sizes,
      control: { type: "inline-radio" },
    },
  }}
/>

# Icon

<ArgsTable of={Icon} />

This is a simple icon component.

export const Template = (args) => ({
  components: { Icon },
  setup() {
    return { args };
  },
  template: '<Icon v-bind="args" @click="action"/>',
});

<Story
  name="Default"
  args={{
    name: "DotsVertical",
  }}
>
  {Template.bind({})}
</Story>
